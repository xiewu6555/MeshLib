# MeshLib 构建配置指南 v2.0

> **版本**: 2.0  
> **更新日期**: 2025-09-13  
> **适用环境**: Windows 11 + Visual Studio 2022 + CMake 3.16+

## 概述

本指南记录了 MeshLib 项目的完整构建配置过程，包括所有依赖问题的解决方案和最佳实践。MeshLib 是一个开源的 3D 数据处理 SDK，支持网格处理、STEP 格式、体素处理等功能。

## 快速开始

### 1. 环境要求

- **操作系统**: Windows 10/11
- **编译器**: Visual Studio 2022 (Community/Professional/Enterprise)
- **CMake**: 3.16 或更高版本
- **Git**: 用于克隆仓库和 vcpkg

### 2. 一键构建脚本

```bash
# 1. 克隆项目
git clone <项目地址> MeshLib
cd MeshLib

# 2. 初始化 vcpkg（如果需要）
git submodule update --init --recursive vcpkg

# 3. 运行一键构建脚本
setup-vcpkg.bat        # 初始化 vcpkg
build_with_manual_occt.bat  # 构建项目（使用手动 OpenCASCADE）

# 4. 运行程序
cd build_final/bin
MeshViewer.exe
```

## 详细配置步骤

### 步骤 1: 项目依赖配置

#### 1.1 vcpkg.json 配置

项目使用 manifest 模式管理依赖，关键依赖包括：

```json
{
  "name": "meshlib",
  "version": "1.0.0",
  "dependencies": [
    "boost-algorithm",
    "eigen3",
    "fmt",
    "glfw3",
    "libe57format",      // E57 点云格式支持
    "xerces-c",          // XML 解析库  
    "gdcm",              // DICOM 医学图像库（用于 MRVoxels）
    "jsoncpp",
    "spdlog",
    "tbb"
  ]
}
```

#### 1.2 OpenCASCADE 手动安装

由于 vcpkg 的 OpenCASCADE 包存在 gettext-libintl 编译问题，采用手动安装方式：

1. **下载 OpenCASCADE 7.9.1**
   ```bash
   # 从 GitHub 下载预编译版本
   # https://github.com/Open-Cascade-SAS/OCCT/releases/tag/V7_9_1
   # 文件: opencascade-7.9.1-vc14-64.exe (约 241MB)
   ```

2. **解压到指定位置**
   ```
   thirdparty/opencascade/occt_vc14-64/
   ├── cmake/
   ├── inc/
   ├── win64/vc14/
   │   ├── bin/    # DLL 文件
   │   └── lib/    # 静态库文件
   ```

3. **验证安装**
   ```bash
   # 检查关键 STEP 支持库
   dir thirdparty\opencascade\occt_vc14-64\win64\vc14\lib\TKDESTEP.lib
   dir thirdparty\opencascade\occt_vc14-64\win64\vc14\lib\TKBRep.lib
   ```

### 步骤 2: CMake 配置

#### 2.1 主要配置文件

**CMakeLists.txt 关键配置**:
```cmake
# 包含手动 OpenCASCADE 配置支持
include(UseManualOpenCASCADE)

# vcpkg 工具链自动检测
if(NOT CMAKE_TOOLCHAIN_FILE)
    set(PROJECT_VCPKG_ROOT "${CMAKE_SOURCE_DIR}/vcpkg")
    if(EXISTS "${PROJECT_VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake")
        set(CMAKE_TOOLCHAIN_FILE "${PROJECT_VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake")
    endif()
endif()
```

#### 2.2 手动 OpenCASCADE 模块

**cmake/Modules/UseManualOpenCASCADE.cmake**:
```cmake
if(USE_MANUAL_OPENCASCADE OR DEFINED OPENCASCADE_MANUAL_ROOT)
    set(OPENCASCADE_MANUAL_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/thirdparty/opencascade/occt_vc14-64")
    set(OpenCASCADE_DIR "${OPENCASCADE_MANUAL_ROOT}/cmake" CACHE PATH "OpenCASCADE CMake 配置目录" FORCE)
    set(MRIOEXTRAS_NO_STEP OFF CACHE BOOL "启用 STEP 格式支持" FORCE)
endif()
```

### 步骤 3: DLL 依赖管理

#### 3.1 自动 DLL 拷贝

**cmake/Modules/CopyRuntimeDependencies.cmake**:
```cmake
function(copy_runtime_dependencies target_name)
    # 拷贝 vcpkg 安装的 DLL
    copy_vcpkg_dlls(${target_name})
    
    # 拷贝手动 OpenCASCADE DLL
    copy_opencascade_dlls(${target_name})
endfunction()
```

#### 3.2 DLL 拷贝结果

成功拷贝的 DLL 文件（共 134 个）：
- **vcpkg 库**: boost、fmt、spdlog、tbb 等
- **OpenCASCADE**: TKDESTEP.dll, TKBRep.dll 等 83 个
- **GDCM**: gdcmCommon.dll, gdcmDICT.dll 等  
- **MeshLib**: MRMesh.dll, MRViewer.dll, MRIOExtras.dll, MRVoxels.dll

### 步骤 4: 构建项目

#### 4.1 CMake 配置

```bash
mkdir build_final
cd build_final
cmake .. -G "Visual Studio 17 2022" -A x64 ^
    -DUSE_MANUAL_OPENCASCADE=ON ^
    -DMRIOEXTRAS_NO_STEP=OFF ^
    -DCMAKE_BUILD_TYPE=Release
```

#### 4.2 编译项目

```bash
# 编译核心模块
cmake --build . --config Release --target MRIOExtras --parallel
cmake --build . --config Release --target MRVoxels --parallel
cmake --build . --config Release --target MeshViewer --parallel

# 或编译整个项目
cmake --build . --config Release
```

## 成功验证

### 1. 构建成功标志

- MeshViewer.exe 生成（约 40KB）
- bin/ 目录包含 134 个 DLL 文件
- 构建日志显示："拷贝 vcpkg 运行时依赖 DLL 文件" 和 "拷贝 OpenCASCADE 运行时依赖 DLL 文件"

### 2. 功能验证

- **STEP 格式支持**: 存在 TKDESTEP.dll
- **体素处理支持**: 存在 MRVoxels.dll 和 gdcmCommon.dll
- **程序运行**: MeshViewer.exe 进程正常启动

### 3. 运行测试

```bash
cd build_final/bin
MeshViewer.exe
# 应该看到 3D 图形界面启动
```

## 常见问题解决

### 问题 1: E57Format.dll 和 Xerces DLL 缺失

**错误现象**:
```
CMake Error: Could not find E57Format.dll
CMake Error: Could not find Xerces Release DLL
```

**解决方案**:
在 `vcpkg.json` 中添加依赖：
```json
"dependencies": [
  "libe57format",
  "xerces-c"
]
```

### 问题 2: OpenCASCADE 找不到

**错误现象**:
```
CMake Error at source/MRIOExtras/CMakeLists.txt:110 (find_package):
  Could not find a package configuration file provided by "OpenCASCADE"
```

**解决方案**:
1. 手动安装 OpenCASCADE 7.9.1 到 `thirdparty/opencascade/occt_vc14-64/`
2. 在 CMake 配置时使用 `-DUSE_MANUAL_OPENCASCADE=ON`

### 问题 3: GDCM 依赖缺失（MRVoxels 模块）

**错误现象**:
```
Could not find a package configuration file provided by "GDCM"
```

**解决方案**:
在 `vcpkg.json` 中添加依赖：
```json
"dependencies": [
  "gdcm"
]
```

### 问题 4: 程序运行时 DLL 缺失

**错误现象**:
双击 MeshViewer.exe 提示缺少 DLL

**解决方案**:
确保 `cmake/Modules/CopyRuntimeDependencies.cmake` 正确配置并在构建时执行

## 最佳实践

### 1. 依赖管理

- 使用项目本地 vcpkg 实例，避免全局环境污染
- 重要依赖使用 manifest 模式（vcpkg.json）管理
- 复杂依赖（如 OpenCASCADE）可考虑手动安装

### 2. 构建配置

- 使用现代 CMake 实践（target-based）
- 合理使用 CMake 模块组织配置逻辑
- 确保 DLL 依赖自动化管理

### 3. 文档维护

- 及时更新构建文档和依赖清单
- 记录问题解决方案供后续参考
- 版本化配置文件和脚本

## 项目结构

```
MeshLib/
├── build_final/              # 构建输出目录
│   └── bin/                 # 可执行文件和 DLL
├── cmake/Modules/           # 自定义 CMake 模块
│   ├── UseManualOpenCASCADE.cmake
│   └── CopyRuntimeDependencies.cmake
├── source/                  # 源代码
│   ├── MRMesh/             # 核心网格库
│   ├── MRViewer/           # 3D 查看器
│   ├── MRIOExtras/         # I/O 扩展（STEP 支持）
│   ├── MRVoxels/           # 体素处理
│   └── MRViewerApp/        # 主界面程序
├── thirdparty/
│   └── opencascade/        # 手动安装的 OpenCASCADE
├── vcpkg/                  # 项目本地 vcpkg
├── CMakeLists.txt          # 主 CMake 配置
├── vcpkg.json             # vcpkg 依赖清单
└── build_with_manual_occt.bat  # 构建脚本
```

## 技术规格

- **C++ 标准**: C++20 (可配置为 C++23)
- **编译器**: MSVC 14.44+ (Visual Studio 2022)
- **CMake**: 3.16+
- **vcpkg**: 最新版本
- **OpenCASCADE**: 7.9.1 (手动安装)
- **支持平台**: Windows x64

## 参考链接

- [MeshLib 官方文档](https://meshlib.io)
- [OpenCASCADE 官网](https://www.opencascade.com/)
- [vcpkg 官方文档](https://vcpkg.io/)
- [CMake 最佳实践](https://cmake.org/cmake/help/latest/)

---

**维护者**: Claude Code Assistant  
**最后更新**: 2025-09-13  
**文档版本**: v2.0